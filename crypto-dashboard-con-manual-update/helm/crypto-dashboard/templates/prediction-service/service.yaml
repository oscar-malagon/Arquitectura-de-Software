apiVersion: v1
kind: Service
metadata:
  name: prediction-service
  labels:
    app: prediction-service
spec:
  type: {{ .Values.predictionService.service.type }}
  ports:
    - port: {{ .Values.predictionService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: prediction-service 