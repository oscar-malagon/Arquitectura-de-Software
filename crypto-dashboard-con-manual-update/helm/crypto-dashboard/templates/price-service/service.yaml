apiVersion: v1
kind: Service
metadata:
  name: price-service
  labels:
    app: price-service
spec:
  type: {{ .Values.priceService.service.type }}
  ports:
    - port: {{ .Values.priceService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: price-service 